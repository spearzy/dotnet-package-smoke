{
  "name": "dotnet-package-smoke",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "package": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=cjs --outfile=dist/index.js --legal-comments=linked",
    "all": "npm run build && npm run test && npm run package"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "description": "",
  "dependencies": {
    "@actions/core": "3.0.1",
    "@actions/exec": "3.0.0",
    "@actions/glob": "0.7.0",
    "adm-zip": "0.5.17",
    "fast-xml-parser": "^5.3.2"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^24.12.2",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "overrides": {
    "undici": "6.24.0"
  }
}
